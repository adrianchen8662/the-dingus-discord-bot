version: '3.8'

services:
  ai-personality-bot:
    build:
      context: .
      dockerfile: Dockerfile-ai-bot
    container_name: ai-personality-bot
    restart: unless-stopped
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - SITE_URL=${SITE_URL}
      - SITE_NAME=${SITE_NAME}
    volumes:
      - ai-bot-data:/app/data
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 256M
          cpus: '0.5'

volumes:
  ai-bot-data: